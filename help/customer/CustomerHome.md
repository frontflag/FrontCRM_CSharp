# 客户首页

## 功能说明

客户模块入口：关键词搜索跳转 **客户列表**（`/customerlist`），下方展示客户、应收款、待出库等概要统计。

## 搜索

- **搜索**：将关键词写入 URL 的 `searchTerm` 后打开列表并检索。
- **进入列表查询**：打开列表且不携带筛选参数。

## 统计

卡片数据来自 `GET /api/v1/customers/statistics`，口径以后端为准。
