---
description: QA 测试对照说明必须先读测试规范总纲并满足全部要求
alwaysApply: true
---

# QA 测试文档（强制）

创建或更新任何 **测试用例 / 测试对照说明**（`document/QA/**/*测试对照说明*.md` 或同类验收文档）时：

1. **必须先用 Read 工具完整阅读** `document/QA/测试规范总纲.md`（不得凭记忆跳过）。
2. **生成内容必须落实该总纲的全部适用要求**（目录命名、文首元信息与交叉引用、推荐结构、§4 通用验收维度、§4.5 金额边界、§5～§6 自检等）。
3. 不适用某一维度时，须在文档中**显式写明「本功能不适用：…」**，不得静默省略总纲已列的强制项（尤其是金额/核销/匹配类的足额成功 + 超额拒绝）。
4. 写完后对照总纲 **§6 编写与评审自检清单** 逐项核对；未满足则不得视为完成。

总纲与 [文档生成规范](document/System/文档生成规范.md) 冲突时：目录/EBS 规则以文档生成规范为准；**测试写法与必测边界以测试规范总纲为准**。
