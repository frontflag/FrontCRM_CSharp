# 服务器部署：复制本文件为与 docker-compose.yml 同目录下的 .env（文件名就是 .env）
# docker compose 会自动读取该目录的 .env，用于替换 compose 里的 ${CONNECTION_STRING}
# 密码中含 # 等特殊字符时，整段连接串用英文双引号包起来。
#
# 示例（请换成你的腾讯云 PostgreSQL 实际参数）：
# CONNECTION_STRING="Host=xxx.sql.tencentcdb.com;Port=25897;Database=FrontCRM;Username=xxx;Password=xxx;SSL Mode=Require;Trust Server Certificate=true"

CONNECTION_STRING=
