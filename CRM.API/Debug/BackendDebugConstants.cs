namespace CRM.API.Debug;

/// <summary>Debug 页展示用后端 API 构建版本（与前端 debugConstants 同步维护，便于核对 API 是否为本次构建）。</summary>
public static class BackendDebugConstants
{
    public const string Version = "1.1.0904-1645 fix(库存): 按单号可查出移库虚拟出库，流程图标注移库";
}
