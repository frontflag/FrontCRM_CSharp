# 行业新闻 — 实施方案

> **状态：** MVP（一期已实现；须执行 SQL、重启 API）  
> **设计对照：** [行业新闻-设计与实现](../System/系统/行业新闻-设计与实现.md)  
> **UI 规范：** [行业新闻简报样式规范](../PRD/规范/UI规范/行业新闻简报样式规范.md)  
> **QA：** [行业新闻-测试对照说明](../QA/系统/行业新闻-测试对照说明.md)  
> **帮助：** [控制台](../../help/pages/控制台_MENU_DASHBOARD.md)、[每日行业新闻](../../help/pages/每日行业新闻_MENU_INDUSTRY_NEWS.md)  
> **增量 SQL：** [PostgreSQL 增量脚本编写规范](../PRD/规范/业务规范/PostgreSQL增量脚本编写规范.md)

每日 08:00 后台生成简报，控制台只读。打开桌面不调模型。

---

## 0. 本期锁定的口径

| # | 议题 | 锁定 |
| --- | --- | --- |
| 1 | 日期窗口 | 结束日=昨天；开始日=昨天往前 1 天 |
| 2 | 谁可见 | 登录即可 |
| 3 | 点击 | 行 / 查看今日 → 当天全文弹层；查看历史 → `/industry-news`；无侧栏菜单、无机构外链 |
| 4 | 输出 | 卡片 JSON + 全文 Markdown；提示词按新模版，6 维 + 五节标题写死 |
| 5 | 厂商名单 | 优先关注样例，不封闭 |

---

## 1. 范围

| 做 | 不做 |
| --- | --- |
| 表 `industry_news_briefing` | 打开桌面现搜 |
| 场景 `industry.news.briefing`（联网） | 按人定制 |
| `GET /api/v1/industry-news` 成功日列表 | 新侧栏菜单 |
| `GET /api/v1/industry-news/latest`、`GET .../{date}` | 失败日期占位 |
| 控制台卡片 + 今日弹层 + 历史页 `/industry-news` | Hangfire |
| 上海 08:00 HostedService | 为新闻单独建参数页 |
| `/system/ai-config` 改模版 + 手跑 | 浏览器新标签打开历史页 |

表只存 `items_json` + `markdown` 信封，不随模版增列。

---

## 2. 上线步骤

1. 执行 `scripts/ensure_industry_news_postgresql.sql`（或跑迁移 `20260914160000_IndustryNewsBriefing`）。  
2. 生产把场景厂商从 `mock` 改为已配置的 `moonshot`（及对应 model），保持 `enable_web_search=true`。  
3. 重启 CRM.API。  
4. 当天 08:00 后等待作业；或由 **SYS_ADMIN** 点控制台「立即刷新」/ `POST /api/v1/industry-news/run`（接口立即返回，后台生成，前端轮询 latest）。  
5. 登录打开控制台，风险预警下方应出现「行业新闻」。  
6. 点「查看历史」应在应用内打开顶栏页签「每日行业新闻」；侧栏无此菜单。

---

## 3. 权限

| 码 | 谁有 | 用途 |
| --- | --- | --- |
| （无额外桌面权限） | 已登录 | 看卡片 / 历史页 / 读 list、latest、按日 |
| `biz.ai.industry_news.briefing` | SYS_ADMIN、biz_all；作业走 `InvokeSystemAsync` | 通用 AI 调用入口 |
| `SYS_ADMIN` 角色 | 系统管理员 | 控制台「立即刷新」/ 手动 `POST .../run` |
| `biz.ai.admin` | AI 管理员 | 改厂商/场景/模板；**不含**手跑 |
