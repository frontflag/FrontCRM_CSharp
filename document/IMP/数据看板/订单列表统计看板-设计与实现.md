# 销售 / 采购订单列表统计看板 — 设计需求与实现方案

> **状态**：已落地（2026-07）  
> **范围**：嵌入 **销售订单列表**、**采购订单列表** 的上下文统计看板（非「报表分析」独立路由）  
> **关联总要求**：复用 [`数据看板总要求.md`](./数据看板总要求.md) §三「四类呈现」与共享前端组件；**不**使用公司/部门/个人 Tab（无 `viewLevel`）  
> **对照**：全局域看板见 [`销售统计.md`](./销售统计.md)、[`采购统计.md`](./采购统计.md)（`/reports/sales`、`/reports/purchase`）

---

## 一、定位与边界

### 1.1 与「报表分析」看板的区别

| 维度 | 列表统计看板（本文） | 报表分析看板（`/reports/*`） |
| --- | --- | --- |
| 入口 | 订单列表页搜索栏「看板」按钮 | 侧栏「报表分析」子菜单 |
| 统计范围 | **与当前列表筛选条件完全一致** | 独立日期 + 公司/部门/个人透镜 |
| 数据权限 | 复用列表 `Apply*OrderDataScopeAsync` | 复用域 Scope + `*AnalyticsScopeValidator` |
| API 前缀 | `/api/v1/sales-orders/analytics/*`、`/api/v1/purchase-orders/analytics/*` | `/api/v1/analytics/sales/*`、`/api/v1/analytics/purchase/*` |
| 典型用途 | 用户在筛完列表后，快速看 KPI/趋势/构成/排行 | 管理层跨周期、跨组织分析 |

### 1.2 产品原则

1. **筛选即统计范围**：看板数字必须与列表筛选结果可对账（日期边界一致）。
2. **销售列表看板（2026-07）**：KPI / 趋势 / 金额类分解 / 排行的订单集合 = **搜索栏筛选结果**，**不再**强制 `status >= 10`；金额含筛选内的取消/审核失败单。
3. **采购列表看板**：仍按 **成单口径**（KPI/金额类默认 `status >= 10`；状态饼图为筛选全状态）。
4. **权限与脱敏**：金额是否展示由 `sales.amount.read` / `purchase.amount.read` 决定；无金额权限时饼图按**订单数**占比。
5. **组件复用**：`AnalyticsKpiGrid`、`AnalyticsTrendChart`、`AnalyticsBreakdownChart`、`AnalyticsBreakdownPieChart`。

---

## 二、交互与 UI

### 2.1 列表页切换

| 项 | 销售订单列表 | 采购订单列表 |
| --- | --- | --- |
| 页面 | `CRM.Web/src/views/RFQ/SalesOrderList.vue` | `CRM.Web/src/views/RFQ/PurchaseOrderList.vue` |
| 看板组件 | `SalesOrderListBoard.vue` | `PurchaseOrderListBoard.vue` |
| 切换按钮 | 搜索栏「看板 / 列表」，`btn-board-active` 样式（两态外观一致） | 同左 |
| `viewMode` | `list` \| `board`，默认 `list` | 同左 |
| 看板模式 | 隐藏顶部 4 张统计卡片；隐藏表格（`v-show`） | 同左 |
| 筛选联动 | `boardFilters` 计算属性随 `filterForm` 变化；看板 `watch` 自动刷新 | 同左 |

### 2.2 看板布局（两域相同结构）

```
┌─ 工具条：口径提示 + groupBy（日/周/月）+ 刷新 ─────────────┐
├─ KPI 区（AnalyticsKpiGrid；销售为筛选结果订单数/金额）────┤
├─ 趋势区：（前端可隐藏）──────────────────────────────────┤
├─ 分解区：状态条图 + 饼图（币别、归属人等）────────────────┤
└─ 排行区：Top10（销售保留业务员；客户维已迁报表）──────────┘
```

**订单金额 KPI**：`AnalyticsKpiGrid` `layout: 'split'` — 左侧折算 USD 大数，右侧原币分列（USD/RMB 等）；无金额权限显示 `—`。

---

## 三、筛选条件（须与列表查询一致）

### 3.1 销售订单

| 查询参数 | 列表字段 | 看板 API 参数 | 备注 |
| --- | --- | --- | --- |
| 订单号 | `code` | `code` | |
| 客户 | `customer` | `customer` | 须 `canViewCustomerInfo`（非 mask521 + 客户读权限） |
| 业务员 | `salesUserName` | `salesUserName` | 须 `!mask521` |
| 备注 | `comment` | `comment` | |
| 状态 | `status` | `status` | 精确匹配 |
| 创建日期 | `startDate` / `endDate` | 同左 | `create_time`，UTC 含当日上界 |

**数据范围**：`IDataPermissionService.ApplySellOrderDataScopeAsync`（与 `SalesOrderListQuery.BuildFilteredQueryAsync` 相同）。

### 3.2 采购订单

| 查询参数 | 列表字段 | 看板 API 参数 | 备注 |
| --- | --- | --- | --- |
| 订单号 | `code` | `code` | |
| 订单类型 | `orderType` | `orderType` | 1 客单 / 2 备货 / 3 样品 |
| 供应商 | `vendor` | `vendor` | 列表侧按 `canViewVendorInfo` 控制输入 |
| 采购员 | `purchaseUserName` | `purchaseUserName` | 须 `canViewPurchaseUser` |
| 货代单号 | `freightForwarderOrderNo` | `freightForwarderOrderNo` | |
| 备注 | `comment` | `comment` | |
| 状态 | `status` | `status` | 精确匹配 |
| 创建日期 | `startDate` / `endDate` | 同左 | `create_time`，UTC 含当日上界 |

**数据范围**：`IDataPermissionService.ApplyPurchaseOrderDataScopeAsync`。

---

## 四、统计口径

### 4.1 销售 vs 采购口径

| 项 | 销售列表看板 | 采购列表看板 |
| --- | --- | --- |
| 订单集合 | **搜索栏筛选结果**（无额外成单约束） | 筛选结果后再 **`status >= 10`**（成单）用于 KPI/金额类 |
| 金额 | 筛选结果 `convert_total`（USD）；可含取消单 | 成单 `convert_total` |
| 时间字段 | `create_time` | 同左 |
| 复购 / 复采 | 按筛选结果计「订单数 ≥ 2」 | 仍按成单数 ≥ 2 |

> 销售：列表看板**不**套用 `/reports` 的成单透镜；用户筛「全部状态」时，取消单计入订单数与金额。  
> 采购：仍保持历史成单口径（本期未改）。  
> `/reports/sales`「客户」Tab 仍为成单（审核通过+）口径，与列表看板职责分离。

### 4.2 日期边界（列表与看板对齐）

创建日期筛选统一为：

- 起始：`SalesAnalyticsDateFilter.ToUtcDateStart` / `PurchaseAnalyticsDateFilter.ToUtcDateStart`
- 截止：`ToUtcDateEndExclusive`（**含选中日全天**，上界为次日 00:00 UTC）

**实现位置**：

- 销售：`CRM.Infrastructure/SalesOrders/SalesOrderListQuery.cs` → `BuildFilteredQueryAsync`
- 采购：`CRM.Infrastructure/PurchaseOrders/PurchaseOrderListQuery.cs` → `BuildFilteredQueryAsync`（2026-07 由 `EndDate.AddDays(1)` 改为与销售一致）

### 4.3 金额脱敏

| 域 | 脱敏门闸 | 金额权限码 | 响应字段 |
| --- | --- | --- | --- |
| 销售 | `SaleSensitiveFieldMask521` | `sales.amount.read` | `context.maskAmounts`；金额为 `null`，前端显示 `—` |
| 采购 | `PurchaseSensitiveFieldMask511` | `purchase.amount.read` | 同左 |

**无金额权限时的分解规则**：

| 图表 | 计量 |
| --- | --- |
| 币别饼图 | 按**订单数** |
| 客户/供应商维度饼图 | 按**订单数**（每条成单订单计 1） |
| 业务员/采购员饼图 | 按**订单数** |
| 订单状态条图 | 按**订单数**（有金额权限时按 `convert_total`） |

---

## 五、指标清单

### 5.1 销售订单列表看板

#### 5.1.1 静态 KPI（Snapshot）

> **口径（2026-07 调整）**：销售列表看板订单集合 = **搜索栏筛选结果**，**不再**强制 `status >= 审核通过`。金额含筛选范围内的取消/审核失败单（若状态筛选未排除）。字段名 `Approved*` 为历史命名，语义已变为「筛选结果汇总」。

| 指标 | 字段 | 口径 | 前端展示 |
| --- | --- | --- | --- |
| 客户数 | `approvedCustomerCount` | 筛选结果 `customer_id` 去重 | **已迁至** `/reports/sales`「客户」Tab（报表侧仍为成单口径） |
| 复购客户数 | `repeatCustomerCount` | 筛选结果中订单数 ≥ 2 的客户 | **已迁至**客户 Tab |
| 订单数 | `approvedOrderCount` | 筛选结果订单数 | ✅ 列表看板保留 |
| 复购订单数 | `repeatOrderCount` | Σ max(0, 客户订单数 − 1) | API 仍返回；列表看板不展示 |
| 订单金额 | `approvedAmountUsd` + `currencyLines[]` | 筛选结果 USD 合计 + 分币别 | ✅ 列表看板保留 |

#### 5.1.2 趋势（Trend）

| 序列 | 字段 | 参数 |
| --- | --- | --- |
| 订单数 | `approvedOrderCount` | `groupBy=day\|week\|month`；集合=筛选结果 |
| 订单金额 | `approvedAmountUsd` | 同左 |

时间桶：有筛选日期用筛选范围；否则用数据 min/max `create_time`。列表看板前端已隐藏趋势图。

#### 5.1.3 分解（Breakdown）

| groupKey | 标签 | 数据范围 | 展示 |
| --- | --- | --- | --- |
| `orderStatus` | 订单主状态 | 筛选结果 | ✅ 列表看板 |
| `currency` | 币别构成 | 筛选结果 | ✅ 列表看板 |
| `customerType` | 客户类型（USD） | 筛选结果 + `customerinfo` | **已迁至**客户 Tab（API 仍可返回） |
| `customerLevel` | 客户等级（USD） | 筛选结果 + `customerinfo` | **已迁至**客户 Tab |
| `customerIndustry` | 客户行业（USD） | 筛选结果 + `customerinfo` | **已迁至**客户 Tab |
| `salesUser` | 业务员（USD） | 筛选结果 | ✅ 列表看板 |

#### 5.1.4 排行（Rankings，Top 10）

| 表格 | 字段 | 展示 |
| --- | --- | --- |
| 客户（金额） | `customerByAmount` | **已迁至**客户 Tab（列表 API 仍按筛选结果计算） |
| 客户（订单数） | `customerByOrderCount` | **已迁至**客户 Tab |
| 客户（复购订单数） | `customerByRepeatOrderCount` | **已迁至**客户 Tab |
| 业务员（金额） | `salesUserByAmount` | ✅ 列表看板保留 |

### 5.2 采购订单列表看板

#### 5.2.1 静态 KPI

| 指标 | 字段 | 口径 |
| --- | --- | --- |
| 成单供应商数 | `approvedVendorCount` | 成单订单 `vendor_id` 去重 |
| 复采供应商数 | `repeatVendorCount` | 成单数 ≥ 2 的供应商数 |
| 成单订单数 | `approvedOrderCount` | `status >= 10` |
| 复采订单数 | `repeatOrderCount` | Σ max(0, 供应商成单数 − 1) |
| 成单金额 | `approvedAmountUsd` + `currencyLines[]` | 同销售 |

#### 5.2.2 趋势

与销售结构相同（`approvedOrderCount`、`approvedAmountUsd`）。

#### 5.2.3 分解

| groupKey | 标签 | 关联字段 | 展示 |
| --- | --- | --- | --- |
| `orderStatus` | 订单主状态 | 全量筛选 | 条图 |
| `currency` | 币别构成（成单） | 成单 | 饼图 |
| `vendorIdentity` | 供应商身份（成单 USD） | `vendorinfo.Credit` | 饼图 |
| `vendorLevel` | 供应商等级（成单 USD） | `vendorinfo.Level`（1=S 2=A 3=B 4=C） | 饼图 |
| `vendorIndustry` | 供应商行业（成单 USD） | `vendorinfo.Industry` | 饼图 |
| `purchaseUser` | 采购员（成单 USD） | 订单头采购员 | 饼图 |

前端字典：`useVendorDictStore`（`identityLabel` / `levelLabel` / `industryLabel`）。

#### 5.2.4 排行（Top 10，三张供应商表 + 采购员）

| 表格 | 字段 | 说明 |
| --- | --- | --- |
| 供应商（成单金额） | `vendorByAmount` | |
| 供应商（成单数） | `vendorByOrderCount` | |
| 供应商（复采订单数） | `vendorByRepeatOrderCount` | 已确认：独立第三张表，不与成单数合并 |
| 采购员（成单金额） | `purchaseUserByAmount` | |

---

## 六、API 设计

### 6.1 端点一览

**销售**（`SalesOrdersController`，须 `sales-order.read`）：

| 方法 | 路径 | 响应 |
| --- | --- | --- |
| GET | `/api/v1/sales-orders/analytics/dashboard` | `SalesOrderListAnalyticsDashboardDto` |
| GET | `/api/v1/sales-orders/analytics/trends` | `SalesOrderListAnalyticsTrendPointDto[]` |
| GET | `/api/v1/sales-orders/analytics/breakdowns` | `SalesAnalyticsBreakdownGroupDto[]` |
| GET | `/api/v1/sales-orders/analytics/rankings` | `SalesOrderListAnalyticsRankingsDto` |

**采购**（`PurchaseOrdersController`，须 `purchase-order.read`）：

| 方法 | 路径 | 响应 |
| --- | --- | --- |
| GET | `/api/v1/purchase-orders/analytics/dashboard` | `PurchaseOrderListAnalyticsDashboardDto` |
| GET | `/api/v1/purchase-orders/analytics/trends` | `PurchaseOrderListAnalyticsTrendPointDto[]` |
| GET | `/api/v1/purchase-orders/analytics/breakdowns` | `SalesAnalyticsBreakdownGroupDto[]` |
| GET | `/api/v1/purchase-orders/analytics/rankings` | `PurchaseOrderListAnalyticsRankingsDto` |

响应包装：`ApiResponse<T>.Ok(data)`（与报表分析 API 风格一致）。

### 6.2 公共 Query 参数

见 §三；趋势额外：`groupBy`（默认 `month`）。

### 6.3 后端分层

```
Controller.BuildListAnalyticsQueryRequestAsync（权限 + 筛选 DTO）
    → IPurchaseOrderListQuery / ISalesOrderListQuery
        → *OrderListQuery.BuildFilteredQueryAsync（数据范围 + 筛选）
        → BuildApprovedOrderQueryAsync（status >= 10）
        → 聚合方法（Dashboard / Trends / Breakdowns / Rankings）
```

**核心约束**：`BuildFilteredQueryAsync` 与列表分页、`GetAggregatesAsync` **共用同一实现**，避免看板与列表条数偏差。

---

## 七、代码索引

### 7.1 后端

| 层级 | 销售 | 采购 |
| --- | --- | --- |
| DTO | `CRM.Core/Models/Analytics/SalesOrderListAnalyticsContracts.cs` | `CRM.Core/Models/Analytics/PurchaseOrderListAnalyticsContracts.cs` |
| 接口 | `CRM.Core/Interfaces/ISalesOrderListQuery.cs` | `CRM.Core/Interfaces/IPurchaseOrderListQuery.cs` |
| 查询实现 | `CRM.Infrastructure/SalesOrders/SalesOrderListQuery.cs` | `CRM.Infrastructure/PurchaseOrders/PurchaseOrderListQuery.cs` |
| Controller | `CRM.API/Controllers/SalesOrdersController.cs` | `CRM.API/Controllers/PurchaseOrdersController.cs` |

共享分解/排行行类型：`CRM.Core/Models/Analytics/SalesAnalyticsContracts.cs`（`SalesAnalyticsBreakdownGroupDto`、`SalesAnalyticsRankingRowDto`）。

日期工具：`CRM.Core/Utilities/SalesAnalyticsDateFilter.cs`、`PurchaseAnalyticsDateFilter.cs`。

### 7.2 前端

| 层级 | 销售 | 采购 |
| --- | --- | --- |
| API 客户端 | `CRM.Web/src/api/salesOrderAnalytics.ts` | `CRM.Web/src/api/purchaseOrderAnalytics.ts` |
| 看板页 | `CRM.Web/src/views/RFQ/SalesOrderListBoard.vue` | `CRM.Web/src/views/RFQ/PurchaseOrderListBoard.vue` |
| 列表宿主 | `CRM.Web/src/views/RFQ/SalesOrderList.vue` | `CRM.Web/src/views/RFQ/PurchaseOrderList.vue` |
| i18n | `salesOrderList.board.*` | `purchaseOrderList.board.*` |
| 共享组件 | `CRM.Web/src/components/Analytics/*` | 同左 |

### 7.3 关联文档

| 文档 | 说明 |
| --- | --- |
| [`数据看板总要求.md`](./数据看板总要求.md) | 四类呈现、组件命名 |
| [`销售统计.md`](./销售统计.md) | 全局销售看板（`/reports/sales`） |
| [`采购统计.md`](./采购统计.md) | 全局采购看板（`/reports/purchase`） |
| [`document/System/销售订单主状态.md`](../System/销售订单主状态.md) | 销售主状态枚举 |
| [`document/System/采购订单主状态.md`](../System/采购订单主状态.md) | 采购主状态枚举 |
| [`document/System/翻页查询规范.md`](../System/翻页查询规范.md) | 列表查询与 `*OrderListQuery` |
| [`document/PRD/规范/UI规范/列表搜索栏规范.md`](../PRD/规范/UI规范/列表搜索栏规范.md) | 搜索栏与看板按钮位置 |

---

## 八、验收要点

1. **筛选一致性（销售）**：同一用户、同一组筛选，列表 `total` 与看板「订单数」一致；状态饼图各分项订单数之和 = 订单数。
2. **日期含当日**：选择结束日为 D，则 D 日 23:59 创建的订单仍计入（UTC 上界 exclusive）。
3. **金额脱敏**：无 `*.amount.read` 时 API 不返回金额；饼图标题带「按订单数」后缀。
4. **采购**：状态饼图与筛选一致；成单 KPI 仍仅 `status >= 10`；供应商三张排行独立展示。
5. **权限过滤**：客户/业务员/供应商/采购员筛选在无权限时不传入 API（与销售/采购列表行为一致）。

---

## 九、非目标与后续扩展

| 项 | 说明 |
| --- | --- |
| 待办 KPI | 列表看板一期不含应收/应付、待入库等待办卡片（见全局看板） |
| 钻取跳转 | 排行行点击跳转列表带筛选 — 二期 |
| 导出 | 看板数据导出 — 二期 |
| 明细行粒度 | 本文仅**订单头**；明细行级看板见 [`销售订单明细列表看板-设计与实现.md`](./销售订单明细列表看板-设计与实现.md)、[`采购订单明细列表看板-设计与实现.md`](./采购订单明细列表看板-设计与实现.md)（**已落地**） |

---

## 十、修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-07-13 | 初稿：销售/采购列表看板设计需求、口径确认（成单 status≥10、采购三表排行、日期 UTC、金额脱敏）、API 与代码索引 |
| 2026-07-13 | §九 补充明细行级看板交叉引用（销售/采购订单明细列表看板已落地） |
