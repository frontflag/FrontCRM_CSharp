# 数据看板 — 总要求（IMP）

> **状态**：方案讨论稿（各业务域 IMP 须引用本文）  
> **关联菜单**：侧栏「报表分析」`/reports`（当前占位，待落地）  
> **子文档**：`销售统计.md`（销售域首期 IMP）

---

## 一、目标

在 CRM 内提供统一的 **数据看板（Analytics Dashboard）** 能力，对销售、采购、物流、财务四条业务线做**带权限过滤**的统计分析，支持 **公司 / 部门 / 个人** 三个层级对象，并按 **静态、趋势、分解、待办** 四类呈现方式组织指标。

**非目标（一期）**：自定义拖拽报表、定时邮件、物理数仓/BI 平台替换、训练型「数据预测」模型上线。

---

## 二、数据角色与权限

### 2.1 三个层级对象

同一业务看板页面通过 **Tab / 分段器** 切换统计透镜（不拆多个路由）：

| 层级 | 统计对象 | 典型用户 |
| --- | --- | --- |
| **公司** | 当前用户数据权限内的「组织整体」汇总 | 管理层、`SaleDataScope=0` |
| **部门** | 按员工**主部门**（`sys_user_department.is_primary`）分组 | 部长、总监 |
| **个人** | 按业务员 / 采购员等**归属人**分组 | 一线销售、采购 |

**原则**：

1. **数据权限是天花板**：看板数据集合 ≤ 对应业务列表在 `DataPermissionService` 下可见集合。
2. **层级是透镜**：在允许的数据内切换聚合维度；**不得**通过前端参数突破 Scope。
3. **后端强校验**：`viewLevel`、`departmentId`、`ownerUserId` 等须由服务端 `*AnalyticsScopeValidator` 校验，越权 **403**。
4. **透明提示**：响应含 `scopeContext`（可见范围文案、Scope 值、是否经权限裁剪）。

各域 Scope 与 Tab 可见性详见 `销售统计.md` §5.3；采购/物流/财务域沿用同一模式，分别映射 `PurchaseDataScope`、`LogisticsDataScope`、`FinanceDataScope`（见 `document/System/权限/权限-部门.md`）。

### 2.2 功能权限

| 项 | 建议 |
| --- | --- |
| 权限码 | 按域拆分：`analytics-sales.read`、`analytics-purchase.read`、`analytics-logistics.read`、`analytics-finance.read` |
| 只读部门 | `*DataAccess=1` 允许看板**只读**访问 |
| 超管 | `SYS_ADMIN` 不受 Scope 限制（与各业务列表一致） |

---

## 三、数据统计方式（呈现模型）

所有业务域指标归入以下 **四类**；API 与前端组件按类复用。

### 3.1 静态数据（Snapshot）

| 项 | 说明 |
| --- | --- |
| **含义** | 到**当前时刻**（或筛选截止时间）的累计/存量统计 |
| **示例** | 订单条目数、销售金额、在库商品数量 |
| **呈现** | **Key : Value**（KPI 卡片、数字面板） |
| **API 形态** | `snapshot.*` 或 `kpi.*` 字段 |
| **刷新** | 用户点击查询 / 进入页面时拉取；不做 WebSocket 实时推送（一期） |

### 3.2 趋势数据（Trend）

| 项 | 说明 |
| --- | --- |
| **含义** | **同一时间段内**随时间变化的数据 |
| **示例** | 近 30 天成单金额；某月至某月应收金额走势 |
| **呈现** | **折线图** 或 **柱状图** |
| **API 形态** | `trend: [{ period, ...metrics }]` |
| **参数** | `dateFrom`、`dateTo`、`groupBy=day\|week\|month` |

### 3.3 分解数据（Breakdown）

| 项 | 说明 |
| --- | --- |
| **含义** | 某一指标的**构成比例** |
| **示例** | 人民币 vs 外币金额占比；订单在各业务环节的状态占比 |
| **呈现** | **饼图**（或环形图）；必要时附明细表 |
| **API 形态** | `breakdown: [{ dimension, value, ratio }]` |
| **约束** | 分项之和 = 100%（或标明「其他」桶） |

### 3.4 待办数据（Todo / Backlog）

| 项 | 说明 |
| --- | --- |
| **含义** | 尚需业务处理的**存量/缺口**指标（可驱动行动） |
| **示例** | 应收款余额、待出库订单条目数、待入库数量 |
| **呈现** | **Key : Value**；可带跳转链接至对应业务列表（带筛选） |
| **API 形态** | `todo.*` 或 `backlog.*` |
| **与静态区别** | 静态偏「结果总量」；待办偏「未完成/待处理」 |

### 3.5 四类与 UI 布局关系

```
┌─ 筛选：日期 / viewLevel / 部门 ─────────────────────────────┐
│ scopeContext 提示条                                          │
├─ 待办区（Todo KPI） ─────────────────────────────────────────┤
├─ 静态区（Snapshot KPI） ─────────────────────────────────────┤
├─ 趋势区（折线/柱状） │  分解区（饼图） ──────────────────────┤
└─ 排行/明细表（可选） ────────────────────────────────────────┘
```

---

## 四、数据统计维度（指标类型）

任意业务指标标注以下**统计维度类型**之一（或多维组合），便于 API 字段命名与前端展示统一。

| 维度类型 | 说明 | 示例 |
| --- | --- | --- |
| **单据数** | 主单或明细**行数** | 需求条目数、报价条目数、销售/采购订单条目数 |
| **数量** | 物料**个数**（含库存 PCS） | 在库商品数量、已出库数量 |
| **金额数** | 货币金额（须标明币别/是否本位币） | 销售金额、采购金额、库存金额、收付款金额 |
| **日期** | 时间长度或账龄类 | 库龄、应收款天数、付款待入库天数 |
| **客户数** | 去重客户主体数 | 需求客户数、成单客户数 |
| **供应商数** | 去重供应商主体数 | 报价供应商数、成单供应商数 |
| **比率** | 两指标之比（转化、完成率） | 需求→销售订单转化率 |

**金额统一约定（建议）**：

- 看板默认展示 **本位币**（`convert_total` / 财务本位汇总字段）。
- 分解图可按 **原币** 或 **币别代码** 分桶。
- 跨域对比（销售 vs 采购）须使用同一汇率快照规则（引用财务参数，二期细化）。

---

## 五、业务统计维度（分域指标目录）

以下按 **销售 → 采购 → 物流 → 财务** 列出指标；每指标须标注 **统计维度类型**（§四）与 **呈现类**（§三）。

图例：`[静]` 静态 `[趋]` 趋势 `[分]` 分解 `[待]` 待办

---

### 5.1 销售

| 指标 | 维度 | 呈现 | 口径要点（实现参考） |
| --- | --- | --- | --- |
| 需求条目数 | 单据数 | 静、趋 | `rfqitem`，未软删；权限 `ApplyRfqMainListDataScopeAsync` |
| 需求客户数 | 客户数 | 静 | 需求关联 `rfq.customer_id` 去重 |
| 需求→销售订单转化比率 | 比率 | 静、趋 | 分子：已关联 `sellorderitem` 的需求明细数；分母：同期需求明细数 |
| 销售订单各环节执行状况 | 单据数/金额 | 分 | 主状态 + `sellorderitemextend` 进度（采购/入库/出库/收款/开票） |
| 销售订单金额 | 金额数 | 静、趋 | 见 **§5.1.1 分阶段金额** |
| 销售订单条目数 | 单据数 | 静、趋 | `sellorderitem` 行数 |
| 销售订单客户数 | 客户数 | 静、趋 | `sellorder.customer_id` 去重 |

#### 5.1.1 销售订单金额 — 分阶段

| 阶段 | 含义 | 数据源建议 |
| --- | --- | --- |
| **成单已审核** | 审核通过后的订单承诺额 | `sellorder.status >= Approved`，`convert_total` |
| **已出库** | 已实际出库对应金额 | `sellorderitemextend` 出库数量 × 单价，或 `stock_out` 已出库单 `total_amount` 汇总 |
| **已收款** | 已核销收款金额 | `sellorderitemextend.receipt_amount_finish` 或财务收款核销汇总 |

各阶段可单独做 `[静]` KPI、`[趋]` 走势、`[分]` 占成单金额比例。

#### 5.1.2 销售待办（示例）

| 指标 | 呈现 | 数据源 |
| --- | --- | --- |
| 应收款金额 | 待 | `receipt_amount_not` / 财务应收 |
| 待出库订单条目数 | 待 | `stock_out_progress_status` 未完成明细数 |
| 待开票金额 | 待 | `invoice_amount_not` |

**首期 IMP**：见 `销售统计.md`。

---

### 5.2 采购

| 指标 | 维度 | 呈现 | 口径要点 |
| --- | --- | --- | --- |
| 报价条目数 | 单据数 | 静、趋 | `quote` / 报价明细行；权限 `ApplyQuoteListDataScopeAsync` |
| 报价供应商数 | 供应商数 | 静 | 报价关联供应商去重 |
| 报价→采购订单转化比率 | 比率 | 静、趋 | 已有 PO 明细关联报价的报价行 / 报价行总数 |
| 采购订单各环节执行状况 | 单据数/金额 | 分 | `purchaseorder.status`、`purchaseorderitem` 入库/付款/出库状态 |
| 采购订单金额 | 金额数 | 静、趋 | 见 **§5.2.1 分阶段金额** |
| 采购订单条目数 | 单据数 | 静、趋 | `purchaseorderitem` |
| 采购订单供应商数 | 供应商数 | 静、趋 | `purchaseorder` 供应商去重 |

> 原文「到家到销售订单转化比率」按业务语义校正为 **报价→采购订单转化比率**；若需「采购→销售」跨域指标，归入二期「链路看板」。

#### 5.2.1 采购订单金额 — 分阶段

| 阶段 | 含义 | 数据源建议 |
| --- | --- | --- |
| **成单已审核** | 采购单审核通过后承诺额 | `purchaseorder.status >= Approved`，`convert_total` |
| **已付款** | 已付款核销金额 | `finance_payment` 核销 / `purchaseorderitem` 付款状态 |
| **已入库** | 已入库对应金额 | 入库单完成数量 × 成本，或 PO 明细入库进度 |

#### 5.2.2 采购待办（示例）

| 指标 | 呈现 | 数据源 |
| --- | --- | --- |
| 应付款金额 | 待 | 财务应付 / PO 待付 |
| 待入库采购明细数 | 待 | `stock_in_status` 未完成行 |

**落地文档**：`采购统计.md`（待编写）。

---

### 5.3 物流

| 指标 | 维度 | 呈现 | 口径要点 |
| --- | --- | --- | --- |
| 在库商品数量 | 数量 | 静、趋 | `stock_item` / `stock` 在库可用；权限 `ApplyStockItemListDataScopeAsync` |
| 在库商品金额 | 金额数 | 静、趋 | 在库数量 × 成本单价（口径需与库存中心一致） |
| 在库商品库龄 | 日期 | 静、分、趋 | 入库日至今日天数分布（桶：0–30/31–90/…） |
| 待入库商品数量 | 数量 | 待、趋 | 已确认 PO/到货通知未入库数量 |

**落地文档**：`物流统计.md`（待编写）。

---

### 5.4 财务

| 指标 | 维度 | 呈现 | 口径要点 |
| --- | --- | --- | --- |
| 已付款金额 | 金额数 | 静、趋 | `finance_payment` 已核销；权限 `FinanceDataScope` |
| 应付（待付）金额 | 金额数 | 待、趋 | 应付余额 |
| 已收款金额 | 金额数 | 静、趋 | `finance_receipt` 已核销 |
| 应收款金额 | 金额数 | 待、趋 | `finance_receivable` 未清余额 |

**落地文档**：`财务统计.md`（待编写）。

---

## 六、技术架构（跨域统一）

### 6.1 路由

```
/reports
├── /sales       销售看板
├── /purchase    采购看板
├── /logistics   物流看板
└── /finance     财务看板
```

### 6.2 API 分层（建议）

每个业务域一组只读端点，**共享查询参数**：

| 端点 | 返回内容 | 对应 §三 |
| --- | --- | --- |
| `GET /api/v1/analytics/{domain}/dashboard` | `scopeContext` + `snapshot` + `todo` | 静态 + 待办 |
| `GET /api/v1/analytics/{domain}/trends` | `trend[]` | 趋势 |
| `GET /api/v1/analytics/{domain}/breakdowns` | `breakdown[]`（可多组） | 分解 |

`{domain}` = `sales | purchase | logistics | finance`

**公共 Query**：

| 参数 | 说明 |
| --- | --- |
| `viewLevel` | `company \| department \| personal` |
| `departmentId` | 部门透镜（须校验） |
| `ownerUserId` | 个人透镜（须校验） |
| `dateFrom` / `dateTo` | 趋势/转化类时间窗 |
| `groupBy` | `day \| week \| month` |
| `metric` | 二期：切换金额阶段（成单/出库/收款等） |

### 6.3 实现分层

```
Controller（鉴权 + ScopeValidator）
    → I*AnalyticsService（Core，指标口径）
        → I*AnalyticsQuery（Infrastructure，聚合 SQL）
            → DataPermissionService.Apply*DataScopeAsync（必须先于聚合）
```

### 6.4 前端组件复用

| 组件 | 用途 |
| --- | --- |
| `AnalyticsScopeTabs` | 公司/部门/个人 Tab + Scope 显隐 |
| `AnalyticsKpiGrid` | Key:Value 静态/待办 |
| `AnalyticsTrendChart` | 折线/柱状 |
| `AnalyticsBreakdownChart` | 饼图 |
| `AnalyticsScopeBanner` | 展示 `scopeContext` |

---

## 七、数据预测

> **一期不实现**；本节预留扩展位。

| 方向 | 说明 | 依赖 |
| --- | --- | --- |
| 销售预测 | 基于历史成单趋势预测下月销售额 | 至少 12 个月 `analytics_sales_daily` 汇总表 |
| 回款预测 | 应收账龄 + 客户历史回款周期 | 财务应收 + 客户维度 |
| 库存预测 | 安全库存、周转天数预警 | 物流库龄 + 出库趋势 |
| 实现形态 | 规则引擎（移动平均/同比）优先；ML 模型二期 | 产品确认后单独立项 |

**前置条件**：静态/趋势/分解指标稳定、日汇总任务跑通、权限与口径验收完成后再做预测。

---

## 八、分期落地建议

| 期次 | 范围 |
| --- | --- |
| **一期** | 销售看板：静态 + 趋势 + 分解 + 部分待办；三层 Tab + `SaleDataScope` |
| **二期** | 采购看板；销售分阶段金额（出库/收款）；跨列表钻取 |
| **三期** | 物流 + 财务看板；库龄/应收天数类指标 |
| **四期** | 数据预测；日汇总表；导出/订阅 |

---

## 九、文档索引

| 文档 | 说明 |
| --- | --- |
| `销售统计.md` | 销售域首期 IMP（三层权限 + 指标落地） |
| `权限-部门.md` | Scope / Access 枚举 |
| `SellOrderItemExtend统计字段文档.md` | 销售环节进度字段 |
| `help/pages/报表分析_MENU_REPORT_ANALYSIS.md` | 用户帮助（待更新） |

---

## 十、修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-06-12 | 初稿：总要求（角色权限、四类呈现、统计维度、四业务域指标目录、预测预留） |
