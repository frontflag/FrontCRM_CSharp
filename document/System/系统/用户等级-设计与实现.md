# 用户等级 — 设计与实现

| 项 | 内容 |
|----|------|
| 状态 | **一期已实现** |
| 业务域 | 系统 / 组织管理 |
| 权限 | 复用 `system.org.users.read` / `system.org.users.write`（与员工管理相同；含 `legacy.rbac.manage` / SysAdmin） |
| 测试对照 | [用户等级-测试对照说明.md](../../QA/系统/用户等级-测试对照说明.md) |
| 帮助 | [用户等级](../../../help/pages/用户等级_MENU_SYS_USER_LEVELS.md) |
| 迁移 | `CRM.Infrastructure/Migrations/20260907120000_UserLevel.cs`、`20260908140000_UserLevelDefinition.cs`（或 `scripts/user_level_def_schema.sql`） |

---

## 一、目标与非目标

### 1.1 目标

1. 每位员工独立维护 **1～20** 的用户等级，新建默认 **1**。
2. **当前等级**写在 `user`：`Level`、`LevelChangedAt`（从未改过级为空）、`LevelRemark`。
3. **变更履历**独立表 `user_level_history`（只追加），供回溯与以后按日取级算提成；**不**写入 `log_operation` / `log_change_fldval`。
4. 组织管理菜单、员工管理之下：路由 `/system/user-levels`（`UserLevelList`），左侧导航「等级列表 / 用户等级」。
5. **等级说明**落库 `user_level_def`（1～20 固定行，仅维护说明）；提成参数页读同一份。

### 1.2 非目标（一期不做）

| 项 | 说明 |
|----|------|
| 员工编辑页改等级 | `/system/users` 新增/编辑不出现等级字段；列表仅只读展示「等级」 |
| 提成计算 | 履历供按日取级；计算见 [业务员与采购员提成-设计与实现](../财务/业务员与采购员提成-设计与实现.md)（草案未落地） |
| 新权限码 | 不新增；与员工管理同一套读写码 |
| 历史回填 1→1 | 上线不插默认履历行 |
| 增减等级档位数 | 不做；档位固定 1～20，不可删行 |

---

## 二、已确认口径

| # | 项 | 决定 |
|---|-----|------|
| 1 | 取值 | 整数 1～20；每人单独改，互不影响 |
| 2 | 默认 | 新建员工 `Level = 1`；`LevelChangedAt` 为空 |
| 3 | 同级保存 | 只更新备注；**不**写履历、**不**改 `LevelChangedAt` |
| 4 | 改级 | 更新 `user.Level` / `LevelChangedAt` / `LevelRemark`，并追加履历一行 |
| 5 | 列表范围 | 与员工列表相同：`GET /api/v1/rbac/admin/users` + `ManagementAccountPolicy`；启用 / 停用 / 冻结都显示 |
| 7 | 等级说明 | 1～20 预置行；说明可空，最长 200；无写权限只读 |
| 6 | 右栏 | 「变更日志」(`r-user-level-log`) + 帮助；门控同操作面板（收起不请求；在帮助页签单击只记行；切到变更日志才拉历史） |

---

## 三、数据模型

### 3.1 `user`（当前值）

| 列 | 说明 |
|----|------|
| `Level` | smallint，1～20，默认 1 |
| `LevelChangedAt` | timestamptz，当前等级开始时间；从未改过级为 NULL |
| `LevelRemark` | varchar(200)，当前备注 |

### 3.2 `user_level_history`（只追加）

| 列 | 说明 |
|----|------|
| `UserLevelHistoryId` | 主键 |
| `UserId` | 被改等级的员工 Id |
| `UserName` | **变更当时登录账号快照**（账号以后改名仍可读；不是实时 JOIN） |
| `OldLevel` / `NewLevel` | 旧级、新级 |
| `Remark` | 当次备注 |
| `ChangeTime` | 新等级开始时刻（UTC） |
| `OperatorUserId` / `OperatorUserName` | 操作人 Id；操作人姓名快照（无姓名则用其账号） |

索引：`(UserId, ChangeTime)`。

### 3.3 `user_level_def`（等级主数据）

| 列 | 说明 |
|----|------|
| `UserLevelDefId` | 主键 |
| `UserLevel` | 1～20，唯一 |
| `Description` | 等级说明，可空，最长 200 |

上线预置 20 行；说明为空。接口在缺行时会补齐，不删行、不改等级数字。

---

## 四、页面与 API

| 项 | 值 |
|----|-----|
| 菜单 | 组织管理 → 用户等级（员工管理之下） |
| 路由 | `/system/user-levels` · `UserLevelList` |
| 列表 | 复用 `GET /api/v1/rbac/admin/users`（`AdminUserDto` 含 `level` / `levelChangedAt` / `levelRemark`） |
| 员工列表 | `/system/users` 增加只读列「等级」；改级仍走用户等级页 |
| 改级 | `PUT /api/v1/user-levels/{userId}` body `{ level, remark }` |
| 履历 | `GET /api/v1/user-levels/{userId}/history` |
| 等级列表 | `GET /api/v1/user-levels/definitions`；`PUT /api/v1/user-levels/definitions/{level}` body `{ description }` |

页面左侧导航（公司信息同款）：

| 导航 | 右侧 |
|------|------|
| 等级列表 | 列：等级、等级说明；操作「编辑说明」（有写权限） |
| 用户等级 | 列：用户名称、用户账号、部门、是否启用、等级、变更日期、备注；操作「设置等级」 |

用户等级列表列：用户名称、用户账号、部门、是否启用、等级、变更日期、备注、操作「设置等级」。默认打开「用户等级」。

检索：用户名称、账号、部门、状态、等级（前端过滤）。状态默认「启用」；重置后仍回到启用。

编辑弹窗：名称/账号只读，等级 1～20，备注最长 200。

右栏「变更日志」：用户信息（当前账号与名称同一行、部门、等级）+ 历次履历（账号与名称同一行、日期与操作人同一行、`{旧} → {新}`；有备注时在变更等级下单独一行）。

列表页遵循 [业务列表规范](../../PRD/规范/UI规范/业务列表规范.md)、[列表搜索栏规范](../../PRD/规范/UI规范/列表搜索栏规范.md)、[列表操作列规范](../../PRD/规范/UI规范/列表操作列规范.md)。无独立详情路由：双击（有写权限）打开编辑弹窗；单击仍用于右栏变更日志。接口暂一次返回全量，前端按筛选结果分页（默认 20 条）。

---

## 五、实现索引

- 常量：`CRM.Core/Constants/UserLevelCode.cs`
- 实体：`CRM.Core/Models/User.cs`、`CRM.Core/Models/System/UserLevelHistory.cs`、`CRM.Core/Models/System/UserLevelDefinition.cs`
- 服务：`CRM.Core/Services/UserLevelService.cs`
- API：`CRM.API/Controllers/UserLevelsController.cs`；列表字段 `RbacController.AdminUserDto`
- 前端：`CRM.Web/src/views/System/UserLevelList.vue`、`components/System/UserLevelChangeLogPanel.vue`、`stores/userLevelLog.ts`
- 单测：`tests/CRM.Core.Tests/Services/UserLevelServiceTests.cs`

---

## 六、修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-19 | 一期：当前等级在 `user`，履历独立表；`UserName` 为当时登录账号快照 |
| 2026-09-08 | 左侧导航拆「等级列表 / 用户等级」；等级说明落库 `user_level_def`；用户列表增加是否启用 |
