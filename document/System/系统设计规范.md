# 系统设计规范

本文档约定系统级设计与实现原则，与具体模块 PRD、实现方案配合使用。

---

## 数据持久化与编辑界面

**除非经产品/架构书面明确同意，禁止通过「将多个编辑界面字段拼接进单一 `comment` / 备注类文本字段」的方式持久化保存。**

说明：

- 包括但不限于：用固定前缀 + 换行、JSON 塞进 varchar、或其它「单字段承载多语义」的约定式拼接。
- 编辑界面中有独立语义的字段，应在数据模型中有**可查询、可校验、可约束长度**的对应存储（独立列、扩展表、或经批准的 JSON 列及 Schema），避免依赖人工解析文本。
- 历史已存在的拼接方案若要延续或迁移，须在对应模块 PRD 与实现方案中**单独立项**并写明兼容与迁移策略。

---

（后续可在此文件追加其它系统级规范条目。）

---

## 数据库增量脚本

手工执行的 PostgreSQL 脚本（`scripts/*.sql`）须遵守 **[PostgreSQL 增量脚本编写规范](../PRD/规范/业务规范/PostgreSQL增量脚本编写规范.md)**，避免 DBeaver / Navicat 弹出「绑定参数」窗口（尤其 AI Prompt 中的 `{{占位符}}` 须用 CHR 拼接或 hex 写入，**注释中也不得出现 `{{`**）。

---

## 前端版本与更新说明

内外版本日志（内部 post-commit 追溯、对外 `/release-notes` 用户说明）的设计要求、文件路径与发布流程见 **[内外版本日志 — 设计与实现](./内外版本日志-设计与实现.md)**。

---

## 文档生成与目录

仓库内 Markdown 技术文档的目录职责、命名及 **`document/EBS/` 只读** 等约定见 **[文档生成规范](./文档生成规范.md)**。生成或更新任何设计/实现/测试文档前须先遵守该规范；**禁止修改 `document/EBS/` 下文件**。
