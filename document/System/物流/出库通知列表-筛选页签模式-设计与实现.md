# 出库通知列表 — 筛选页签模式 — 设计与实现

**状态：** 已上线  
**页面：** `/stock-out-notifies`、`/inventory/stock-out-notifies`（同一组件；搜索区齿轮 →「页签模式」）  
**UI 规范：** [列表搜索栏规范 §10](../../PRD/规范/UI规范/列表搜索栏规范.md)  
**测试对照：** [出库通知列表-筛选页签模式-测试对照说明](../../QA/物流/出库通知列表-筛选页签模式-测试对照说明.md)  
**交互参考：** [销售订单明细列表-筛选页签模式-设计与实现](../销售/销售订单明细列表-筛选页签模式-设计与实现.md)

---

## 1. 业务定位

将搜索栏中适合做成单选桶的下拉，可选转为表格上方全宽页签条。**单维度**；关闭页签不清筛选值；点页签立即查询。

文本、日期不纳入。本列表无左栏 shortcut preset。首版**只做页签模式**，不改日期控件形态、不下拉变更即查。

---

## 2. 设计要求（定稿）

### 2.1 可纳入维度

| 维度 key | 搜索栏字段 | 页签形态 |
|----------|------------|----------|
| `status` | `status` | 全部 + 待报关 / 待装箱 / 已装箱 / 已出库 / **已取消**（与下拉一一对应） |
| `regionType` | `regionType` | 全部 + 境内 / 境外 |
| `stockOutType` | `stockOutType` | 全部 + 销售 / 报关 / 退货 / 报废 |

### 2.2 交互与视觉

遵循 UI 规范 §10：齿轮菜单、单维度、贴合表顶、亮色未选中 `#E8EDF5`、暗色 `var(--crm-layer-1)`。两路由共用偏好键。

---

## 3. 实现方法

| 文件 | 职责 |
|------|------|
| `CRM.Web/src/utils/stockOutNotifyListTabMode.ts` | 维度、localStorage、`tab↔filter` |
| `CRM.Web/src/views/RFQ/StockOutNotifyList.vue` | 菜单、页签条、按维度隐藏下拉 |
| `zh-CN.ts` / `en-US.ts` | `settingsMenu.*`、`filterTabs.all` |

**localStorage：** `crm.stock-out-notify-list.tab-mode` = `off` \| `status` \| `regionType` \| `stockOutType`。

**无需改后端**：同一套列表 query（`status`、`regionType`、`stockOutType`）。

---

## 4. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-18 | 首版：状态（含已取消）/ 地域 / 出库类型；仅页签模式 |

---

*文档遵循 [文档生成规范](../文档生成规范.md)。*
